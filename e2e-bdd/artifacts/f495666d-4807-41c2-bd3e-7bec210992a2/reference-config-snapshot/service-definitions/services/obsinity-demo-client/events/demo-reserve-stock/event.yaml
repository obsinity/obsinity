apiVersion: obsinity/v1
kind: Event
metadata:
  service: obsinity-demo-client
  name: demo.reserve.stock
  displayName: Demo Reserve Stock Step
  labels:
    category: demo
spec:
  retention:
    ttl: "7d"
  schema:
    type: object
    properties:
      sku: { type: string, index: true }
      outcome: { type: string, index: true }
